programa testeExpressoes
    variavel a,b: real;
    variavel c,d,e,f,g: real;
    variavel h,i,j: real;
    variavel k,l,m: real;
    variavel n,o,p: real;
    variavel q: real;
    variavel x1, var2: real;
    variavel r7: real;
    inicio
    a := 5 * ((2 + 10) + 5.2);
  	b := 5 * (2 + 10.1) + 5;
  	c := 2 + 5 * 23 + 6 / 3 - 2 * -1; 
    d := (5 * 2 + 11) + 5;
    e := (1+4) + (2 + 2);
    f := (1+9) * (2 + 2);
    g := (1+3) + (2 + 2) + (3 - 1);
    h := (1+5) + (2 + 2) - (3 - 1);
    i := (1+1) + (2 + 2) * (10 / 3);
    j := (1+1) * (2 + 2) + (10 / 7);
    k := (1+1) * (2 + 2) * (10 / 11);
    l := (1+1) * (2 + 2) / (10 / 22);
    m := (1+1) + (2 + 2) * (10 / 15) - 2;
    n := (1+1) + (2 + 2) * (10 / 2) - (2 + 2);
    o := ((1+1) + (2 + 2)) * (10 / 5) - (2 + 2);
    p := (1+1) + (2 + 2) * ((10 / 4) - (2 + 2));
    q := (1+1) + 2 + (2 * ((10 / 8) - (2 + 2)));
    x1 := a + b * (4 - 1);
    var2 := x1 + x1;
    r7 :=  var2 / 2;
    fim
fimprog