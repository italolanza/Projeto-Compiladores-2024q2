programa meuPrograma
	variavel a,b: inteiro;
	variavel c,d: real;
	variavel x,y: texto;
	inicio
		a := 2;
		b := -1;
	
	
		se ( (b + 1) >= 0 ) entao
			se ( (b + 2) >= 0 ) entao
				b := 3;
			fimse
		fimse
		
		se ( (b + 1) >= 0 ) entao
			se ( (b + 2) >= 0 ) entao
				se ( (b + 3) >= 0 ) entao
					b := 42;
				fimse
			fimse
		fimse
	
		se (a > 5) entao
			se ( (b + 2) >= 2 + 4 ) entao
				b := 10;
			fimse
		senao
	    	se ( (b + 1) >= 0 ) entao
				b := 3;
			fimse
		fimse
		
		se ( (b + 2) >= 0 ) entao
			b := 10;
		senao
			se ( (b + 1) >= 0 ) entao
				se ( (b + 2) >= 0 ) entao
					se ( (b + 3) >= 0 ) entao
						b := 42;
					fimse
				fimse
			fimse
		fimse
		
		se (a > 5) entao
			se (a < b) entao
				b := a;
			senao
				a := b;
			fimse
		senao
			se (a < b) entao
				b := a;
			senao
				a := b;
			fimse
		fimse
		
		
		se (a > 5) entao
			se (a < b) entao
				se (a > 5) entao
					se (a < b) entao
						b := a;
					senao
						a := b;
					fimse
				senao
					se (a < b) entao
						b := a;
					senao
						a := b;
					fimse
				fimse
			senao
				a := b;
			fimse
		senao
			se (a < b) entao
				b := a;
			senao
				a := b;
			fimse
		fimse
	fim
fimprog